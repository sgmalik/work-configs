S-CAF not putting allowed_tools in SKILL.md frontmatter or generating a skill.yaml

Description

Expected Result

Status

Scenario 1: Declared allowed tools are the only tools available to the AI

User Role: Skill Author configuring a skill's allowed tools (skill.yaml) and running it in S-CAF

1. In skill.yaml, declare allowed_tools (e.g., read_file, list_directory, run_sas).
2. Run the skill and have the AI perform actions using the allowed tools.

Only the tools declared in allowed_tools are available to the AI at runtime.

The AI can use the intended tools (e.g., read a file, run SAS) successfully.

[US 206044]

Not Run

Scenario 2: A non-allowed tool call is blocked

User Role: Skill Author configuring a skill's allowed tools (skill.yaml) and running it in S-CAF

1. Keep allowed_tools limited (e.g., read_file only).
2. Prompt the AI to perform an action needing a tool NOT in the list (e.g., write or delete a file).

The call to a non-allowed tool is blocked.

The AI cannot perform operations outside the declared allow list.

[US 206044]

Not Run

Scenario 3: No allowed tools - AI can only respond with text

User Role: Skill Author configuring a skill's allowed tools (skill.yaml) and running it in S-CAF

1. Set allowed_tools to empty for the skill.
2. Run the skill and prompt the AI to take an action.

With no tools allowed, the AI can only respond with text and cannot take any actions.

[US 206044]

Not Run

Scenario 4: Reviewing a skill shows exactly what tools it can use

User Role: Skill Author configuring a skill's allowed tools (skill.yaml) and running it in S-CAF

1. Open the skill's configuration / skill.yaml.
2. Review the allowed_tools declaration.

The skill config clearly shows exactly which tools the skill is permitted to use.

[US 206044]

Not Run

Scenario 5: Allowed tools are enforced consistently across runs

User Role: Skill Author configuring a skill's allowed tools (skill.yaml) and running it in S-CAF

1. Re-run the skill after confirming the allow list.
2. Repeat an allowed and a non-allowed tool call.

Allowed tools continue to work and non-allowed tools continue to be blocked on repeated runs - enforcement is consistent.

[US 206044]

Not Run