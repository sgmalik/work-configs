CLI context does not update when navigating left panel

Bug Description:
Navigating the left side while the CLI is open does not update the CLI context.

Expected Behavior:
CLI context should update when user navigates in the left panel.

Steps to Reproduce:
- Open CLI
- Navigate to different item in left panel
- Observe CLI context does not change