>When workflow steps and other required metadata are created with the skill- prefix, Night Shift mode is not triggered or executed as expected. Despite the required skill- prefixed configuration being present, the feature fails to recognize the wor

Issue Description: When workflow steps and other required metadata are created with the skill- prefix, Night Shift mode is not triggered or executed as expected. Despite the required skill- prefixed configuration being present, the feature fails to recognize the workflow and does not operate in Night Shift mode.

Testing Location- https://cdrsce-syc16-staging.sycamoreinformatics.com/app/studies/3710/deliverables

Steps to reproduce
1. Create Module Check with prefix text as skill.
2. Create Workflow Steps with a prefix text as a skill.
3. Create a workflow using steps with a prefix text as a skill.
4. Create task using this workflow.

Note- I am not sure where to check and how to check the nightshift mode is running or not.

Expected Result- Nightshift mode should be executed successfully.