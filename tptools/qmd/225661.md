When I choose from the Hamburger menu against a file, if the skill is not appropriate for the file the list should not appear or at least not begin the TUI/CLI

Opens CLI for City Details on a SAS&BDAT file and attempts to make sense of it but eventually fails LLM.  Should just not run or not be in the skills list for that type of file.