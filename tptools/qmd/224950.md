PII block happens even when "Off".

Description

Expected Result

Status

Scenario 1: Default Warn - PII in prompt is flagged and recorded (types only), not blocked

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Ensure the agent's Input PII detection is at the default (Warn).
2. Submit a prompt containing likely PII/PHI (e.g., an SSN or MRN).
3. Run the agent and review the run record.

The run is flagged 'possible sensitive data in prompt' (warn) and the PII types found are recorded - never the values.

The prompt is not blocked; the AI call still proceeds.

[US 212485]

Passed

Scenario 2: Block setting - prompt with PII stopped before the AI call

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Set the agent's Input PII detection to Block.
2. Submit a prompt containing PII.
3. Run the agent.

The run is stopped before the AI call with a clear message that sensitive data was detected in the prompt.

[US 212485]

Failed

Scenario 3: Off setting - no detection occurs

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Set the agent's Input PII detection to Off.
2. Submit a prompt containing PII.
3. Run the agent.

With detection Off, no PII detection occurs and the prompt proceeds without flag or block.

[US 212485]

Failed

Scenario 4: Guardrails form offers Off / Warn / Block (default Warn)

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Open the agent's guardrails configuration form.
2. Locate the Input PII detection setting.

The form lets the admin choose Off / Warn / Block for Input PII detection, with Warn as the default.

[US 212485]

Not Run

Scenario 5: Legitimate clinical/CDISC content is not falsely flagged

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. At the default Warn setting, submit a prompt that legitimately references CDISC/stat values (e.g., USUBJID, counts) or a safe-domain email.
2. Run the agent.

Legitimate clinical/CDISC content and safe-domain emails are not falsely flagged - false-positive suppression and safe-domain allowlisting work.

[US 212485]

Not Run

Scenario 6: Detected identifier types are recorded without values

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Submit prompts containing different identifiers (SSN, email, phone, DOB, MRN, patient/subject name).
2. Review the recorded detection metadata.

The record captures the PII types detected but never stores the actual values.

[US 212485]

Not Run

Scenario 7: Output-side PHI/PII remains always-blocked (separate control)

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Cause the AI to attempt to include PII/PHI in its output.
2. Observe output-side handling.

Output-side PHI/PII leakage continues to be always-blocked separately from the input-side detection.

[US 212485]

Not Run

Scenario 8: Input detection closes the input-screening gap (ADR-054 / PR #197)

User Role: S-CAF Admin User configuring and reviewing input PII/PHI detection on an agent's guardrails in S-CAF / CDR-SCE

1. Confirm input prompts are now screened for PII (previously only output was screened).
2. Review that input screening is active per configuration.

Input prompts are now screened for PII/PHI, closing the prior gap where only AI output was screened.

[US 212485]

Not Run