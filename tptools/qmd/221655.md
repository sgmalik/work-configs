Observation:- Execution Log table is not immutable

Currently only the Audit table is immutable and the execution log table can be updated since it is similar to an audit log can be made as immutable. Opening this as an observation.